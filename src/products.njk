---
layout: base.njk
title: QUARX Products - Accelerated Analytics & Insight Generation
description: Explore QUARX's analytics products - CHORD for reimbursement data, BASIS for access strategy simulation, and LARA for AI-enabled launch planning
---

<main class="bg-white min-h-screen flex flex-col">
  <div class="flex min-h-px w-full bg-[#E2E2E9]"></div>

  <main class="min-h-[90vh] py-8 md:py-12 px-2.5 flex flex-col justify-center items-center gap-10">
    <div class="w-full max-w-[1440px] px-2.5 py-12 overflow-hidden flex flex-col justify-center items-center gap-10">
      <div class="flex flex-col justify-start items-center gap-8">
        <h1 class="text-center text-[#0B3041] text-4xl md:text-5xl font-medium leading-tight">
          Our Products: for Accelerated Analytics & Insight Generation
        </h1>
      </div>

      <div class="w-full flex flex-col lg:flex-row justify-start items-center gap-5">
        {% for product in products %}
        <div class="flex-1 p-5 bg-[#F1F4F1] shadow-[0px_3px_8px_rgba(0,0,0,0.24)] rounded-[20px] flex flex-col justify-start items-center gap-8 w-full">
          <h2 class="text-[#0B3041] text-4xl md:text-5xl font-medium leading-tight">
            {{ product.title }}
          </h2>

          <div class="w-full text-center text-[#5A9BA6] text-2xl md:text-3xl font-medium leading-10 whitespace-pre-line">
            {{ product.subtitle }}
          </div>

          <div class="w-[85px] h-[85px] flex justify-center items-center">
            <img
              src="{{ product.icon }}"
              alt="{{ product.title }} icon"
              class="w-[85px] h-[85px]"
            />
          </div>

          <div class="w-full flex justify-center items-start gap-2.5">
            <p class="flex-1 text-center text-[#0B3041] text-xl md:text-2xl font-medium leading-9">
              {{ product.description }}
            </p>
          </div>

          <a
            href="/products/{{ product.slug }}/"
            class="w-[58px] h-[58px] relative overflow-hidden rounded-full bg-[#5A9BA6] flex items-center justify-center hover:bg-[#5A9BA6]/80 transition-colors cursor-pointer"
            aria-label="Learn more about {{ product.title }}"
          >
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7" />
            </svg>
          </a>
        </div>
        {% endfor %}
      </div>
    </div>
  </main>
</div>
